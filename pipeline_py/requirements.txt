# 텍데분 파이프라인 — 같은 결과가 나오도록 버전을 맞춰 둠. Python 3.11.
# 로컬(WSL) news-analysis env에서 검증된 버전. 학교(GPU) 이식 시 아래 순서로:
#   1) python 3.11 가상환경
#   2) pip install -r requirements.txt
#   3) torch: 아래 주석 참고(로컬=CPU, 학교=GPU 선택)
#   4) HF 모델 2개 1회 다운로드(dl_tone/nli_stance, local_files_only=True):
#        python -c "from transformers import AutoTokenizer,AutoModelForSequenceClassification as M; \
#          [ (AutoTokenizer.from_pretrained(x), M.from_pretrained(x)) for x in \
#            ('snunlp/KR-FinBert-SC','pongjin/roberta_with_kornli') ]"

pandas==3.0.3
numpy==2.2.6
scipy==1.13.1
gensim==4.4.0
kiwipiepy==0.23.1
scikit-learn==1.8.0
wordcloud==1.9.6
matplotlib==3.10.9
emoji==2.15.0
seaborn==0.13.2
networkx==3.6.1
tabulate==0.10.0
nbconvert==7.17.1
nbformat==5.10.4

# --- 딥러닝 추론(dl_tone, nli_stance) ---
# 아래 버전으로 GPU(RTX 4070 Ti)·CPU 양쪽 검증 완료 — DL/NLI 라벨이 팀 ref와 100% 일치, 확률차 ≤1.8e-5(§9 허용오차)
# CPU 전용 설치가 필요하면: pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
# 모델은 코드가 local_files_only=True라 1회 선다운로드 필수(상단 주석의 python -c 명령)
transformers==5.10.2
torch==2.12.0
